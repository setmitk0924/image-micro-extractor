name: my_flutter_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  image: ^3.0.1 
  flutter:
    sdk: flutter
  image_picker: ^0.8.4+4 # Replace with the actual version
  cupertino_icons: ^1.0.8
  file_picker: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
